Workshop 5
practicing lambda expression, try/catch, throw, template member function/template function, enumeration
editing decimal point, I can use std::cout.precision that works as a member function OR std::setprecision(int n) that is IO manipulator which requires #include <iomanip> header.
std::stod - converting string type to double type.
size_t - Unsigned integral type: Alias of one of the fundamental unsigned integer types. It is a type able to represent the size of any object in bytes: size_t is the type returned by the sizeof operator and is widely used in the standard library to represent sizes and counts.